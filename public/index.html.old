<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>GetVisa - Dashboard Central</title>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: #f0f2f5;
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 20px;
        }
        .dashboard {
            max-width: 1000px;
            width: 100%;
            background: white;
            border-radius: 16px;
            box-shadow: 0 4px 20px rgba(0,0,0,0.1);
            padding: 40px;
        }
        .dashboard h1 {
            color: #003366;
            font-size: 28px;
            text-align: center;
            margin-bottom: 10px;
        }
        .dashboard .subtitle {
            text-align: center;
            color: #666;
            margin-bottom: 30px;
            font-size: 16px;
        }
        .grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 20px;
        }
        .card {
            background: #f8f9fa;
            border-radius: 12px;
            padding: 25px 20px;
            text-align: center;
            transition: all 0.3s ease;
            border: 2px solid transparent;
            text-decoration: none;
            color: #333;
            cursor: pointer;
        }
        .card:hover {
            border-color: #003366;
            transform: translateY(-4px);
            box-shadow: 0 8px 20px rgba(0,51,102,0.1);
        }
        .card .icon {
            font-size: 40px;
            margin-bottom: 10px;
        }
        .card h3 {
            font-size: 18px;
            color: #003366;
            margin-bottom: 5px;
        }
        .card p {
            font-size: 14px;
            color: #666;
        }
        .card .badge {
            display: inline-block;
            background: #003366;
            color: white;
            padding: 2px 12px;
            border-radius: 20px;
            font-size: 11px;
            margin-top: 8px;
        }
        .card .badge.publico {
            background: #28a745;
        }
        .card .badge.novo {
            background: #ffc107;
            color: #333;
        }
        .footer {
            text-align: center;
            margin-top: 30px;
            color: #999;
            font-size: 13px;
            border-top: 1px solid #eee;
            padding-top: 20px;
        }
        .logout-btn {
            display: inline-block;
            margin-top: 10px;
            color: #dc3545;
            text-decoration: none;
            font-weight: 600;
            cursor: pointer;
        }
        .logout-btn:hover { text-decoration: underline; }
        .version {
            display: block;
            margin-top: 5px;
            font-size: 11px;
            color: #aaa;
        }
        @media (max-width: 600px) {
            .dashboard { padding: 20px; }
            .grid { grid-template-columns: 1fr; }
        }
    </style>
    <link rel="icon" href="/favicon.ico" type="image/x-icon">
</head>
<body>
<div class="dashboard">
    <h1>📊 GetVisa - Central de Controle</h1>
    <p class="subtitle">Acesse todos os painéis administrativos em um só lugar</p>

    <div class="grid">
        <!-- 👥 CLIENTES - Link direto -->
        <a href="/painel?api_key=admin123" class="card">
            <div class="icon">👥</div>
            <h3>Clientes</h3>
            <p>Visualize e gerencie todos os clientes</p>
            <span class="badge">Ativo</span>
        </a>

        <!-- 📅 AGENDAMENTOS - Link direto -->
        <a href="/admin.html?api_key=admin123" class="card">
            <div class="icon">📅</div>
            <h3>Agendamentos</h3>
            <p>Gerencie agendamentos e treinamentos</p>
            <span class="badge">Ativo</span>
        </a>

        <!-- 📋 FORMULÁRIO - público -->
        <a href="/formulario-ds160" class="card">
            <div class="icon">📋</div>
            <h3>Formulário DS-160</h3>
            <p>Preencher novo formulário</p>
            <span class="badge publico">Público</span>
        </a>

        <!-- 📊 SIMULADOR - público -->
        <a href="/simulador-visto-americano" class="card">
            <div class="icon">📊</div>
            <h3>Simulador de Perfil</h3>
            <p>Avaliação gratuita de perfil</p>
            <span class="badge publico">Público</span>
        </a>

        <!-- 📤 UPLOAD CASV - Link direto -->
        <a href="/upload-casv-pdf?api_key=admin123" class="card">
            <div class="icon">📤</div>
            <h3>Upload CASV PDF</h3>
            <p>Envie PDF de agendamento</p>
            <span class="badge">Ativo</span>
        </a>
    </div>

    <div class="footer">
        <p>GetVisa Assessoria - Sistema de Gestão</p>
        <span class="version">v2.0 - Atualizado em 31/08/2026</span>
        <a href="/admin-login.html" class="logout-btn">🚪 Sair</a>
    </div>
</div>

<script>
    // Verifica se está autenticado
    document.addEventListener('DOMContentLoaded', function() {
        const apiKey = sessionStorage.getItem('adminKey');
        if (!apiKey) {
            // Se não tem chave, redireciona para o login
            window.location.href = '/admin-login.html';
        }
    });
</script>
</body>
</html>